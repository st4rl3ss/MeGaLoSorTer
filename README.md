# MeGaLoSorTer
MeGaLoSorTer generates MiSTer .mgl launchers from ROM sets + PigSaint/GameDataBase CSV metadata. Sorts games into menu-browsable folders (publisher/dev/genre/date), hashes ROMs for matching, and supports fast re-runs via SQLite cache.
